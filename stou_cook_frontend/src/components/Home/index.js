import React, { Component } from 'react';
import Login from '../Login';

export class Home extends Component {
    render() {
        return (
            <div>
                <Login />
            </div>
        );
    }
}

export default Home;